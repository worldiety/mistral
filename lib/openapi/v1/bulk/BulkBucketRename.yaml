type: object
required:
  - old
  - new
properties:
  old:
    type: array
    items:
      $ref: '../uuid/UUID.yaml'
    description: Represents UUIDs of old bucket ids.
    nullable: false
  new:
    type: array
    items:
      $ref: '../uuid/UUID.yaml'
    description: Represents UUIDs of new bucket ids.
    nullable: false